name: Scoring
on: pull_request
jobs:
  score_job:
    runs-on: ubuntu-latest
    outputs:
      score: ${{ steps.score_step.outputs.finalscore }}
    steps:
      - uses: actions/checkout@v2
      - id: score_step
#        run: echo "::set-output name=finalscore:$(bash ./test_scores.sh)"
        run: echo "::set-output name=finalscore::20"
        working-directory: scoring
  comment:
    runs-on: ubuntu-latest
    needs: score_job
    steps:
      - uses: actions/checkout@v2
      - uses: thollander/actions-comment-pull-request@master
        with:
          message: "Current score: ${{ needs.score.outputs.score }}"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: echo ${{needs.score_job.outputs.score}}
